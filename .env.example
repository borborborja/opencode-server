# Basic-auth del servidor opencode (usuario por defecto: opencode)
OPENCODE_SERVER_PASSWORD=changeme

# Proveedor(es) LLM
OPENROUTER_API_KEY=

# Identidad git del agente
GIT_USER_NAME=opencode
GIT_USER_EMAIL=agent@opencode.local

# Actualizar opencode en cada arranque
AUTO_UPDATE=true
